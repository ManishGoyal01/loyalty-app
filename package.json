{
  "name": "loyalty-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd server && npm install && npx prisma generate && npx prisma migrate deploy && cd ../client && npm install && npm run build && cp -r dist ../server/public",
    "start": "node server/server.js"
  }
}
