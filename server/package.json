{
  "name": "loyalty-streak-server",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "prisma:generate": "prisma generate",
    "dev": "nodemon server.js",
    "build:client": "cd ../client && npm install && npm run build",
    "railway:build": "npm install && npx prisma generate && npx prisma migrate deploy && npm run build:client && cp -r ../client/dist ./public"
  },
  "dependencies": {
    "@prisma/client": "5.22.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2",
    "prisma": "5.22.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
